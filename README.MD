run the following (don't forget to insert your git credentials where needed)::

git clone https://github.com/zalaby12/vim.git; cd vim; ./installSoftwareFromScratch "git user name" "git email"; cd ..; rm -rf vim
